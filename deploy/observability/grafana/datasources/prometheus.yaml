# Auto-provisioned Prometheus datasource — Grafana picks this up on
# first boot via /etc/grafana/provisioning/datasources/.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "5s"
